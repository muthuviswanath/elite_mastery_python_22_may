# create a class called product
# create the necessary fields
#  while calculating the tota for the quantity, if the quantity is less than 5
# raise Value
a = int(input())
b = list(a)
print(a.getKey())